apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim
  labels:
    app: mysql
  namespace: projet-eni
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi  
  resources:
    requests:
      storage: 2Gi
